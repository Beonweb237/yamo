# Prompt Master — Corrections qualité pages (QA-A → QA-I)

Chaque section est un **prompt autonome** à donner tel quel à Claude Code (une session ou un message par lot). Les constats Cxx renvoient à `app/docs/audit-qualite-pages.md`. L'ordre, les portes de vérification et le suivi sont dans `app/docs/prompt-master-coordination.md`.

**Préambule commun (inclus par référence dans chaque lot)** :
> Travaille dans `app/`. Respecte CLAUDE.md : périmètre strict du lot, réutilise `src/components/ui/` et les tokens (`green-primary` #157F3D, `gold-accent` #D4A843), aucune nouvelle dépendance, aucune fonctionnalité factice, pas de refonte au-delà du lot. Après modification : `npm run lint` (zéro NOUVELLE erreur sur les fichiers touchés — comparer avant/après, ~79 erreurs pré-existantes assumées), `npm run build`, puis vérification navigateur sur le serveur mock (config `yamo-web-mock-3011`, seed auto) en desktop 1280px ET mobile 360×640 (aucun débordement horizontal). Ne pas toucher aux branches `isSupabaseConfigured` des libs.

---

## QA-A — Brancher réellement le formulaire Contact (C1) — P0

> **[Préambule commun]** Dans `src/pages/Contact.tsx`, le formulaire « Envoyez-nous un message » est factice : `handleSubmit` (l.~95) affiche une confirmation 5 s sans rien envoyer. Corrige-le pour qu'il fasse quelque chose de réel, sans backend : à la soumission, construis un message WhatsApp pré-rempli à partir des champs (nom, email, téléphone, sujet, message) et ouvre `https://wa.me/237677777777?text=<message encodé>` dans un nouvel onglet — WhatsApp est déjà le canal principal de la page et du contexte camerounais. Ajoute sous le bouton une alternative `mailto:support@miamexpress.cm` avec sujet/corps pré-remplis. Adapte le message de confirmation pour dire la vérité (« Votre message est prêt dans WhatsApp — appuyez sur Envoyer »). Conserve la validation des champs requis et les états du bouton. Vérifie : soumission ouvre bien wa.me avec le texte complet encodé, mobile 360px OK.

## QA-B — Rendre `/commandes` lisible (C2) — P0

> **[Préambule commun]** Dans `src/pages/Orders.tsx` : (1) Ajoute le **nom du restaurant** en tête de chaque carte de commande (donnée `order.restaurantName`, déjà présente) — visible sans ouvrir le détail. (2) Le **stepper de progression** (Confirmée → En préparation → Prête → En livraison → Livrée) ne doit s'afficher QUE pour les commandes actives (`pending`→`delivering`) ; pour `delivered`/`cancelled`, remplace-le par un simple badge de statut — l'historique de 20 commandes ne doit plus répéter 5 étapes par carte. (3) Remplace l'identifiant tronqué illisible (« #seed-dem… ») par un **numéro court stable** dérivé de l'id (ex. `#Y-` + 4 derniers caractères alphanumériques en majuscules) affiché partout où l'id apparaît dans la page. (4) Corrige les accents des toasts (« enregistré »). Ne change ni la logique de re-commande ni la notation. Vérifie avec le compte seed Marie Ngono (+237650000001, 20 commandes) : chaque carte montre restaurant + numéro court + badge, les actives gardent le stepper.

## QA-C — Honnêteté marketing des pages vitrines (C3, C4, C7, C8, C14) — P0

> **[Préambule commun]** **Règle « données à compléter plus tard » : ne rien supprimer qui attend une vraie valeur (liens stores, paiement carte…) — neutraliser proprement et centraliser dans `src/data/launchConfig.ts` (constantes nullables commentées « À compléter ») pour qu'une seule ligne réactive l'élément au lancement.** Sur `src/pages/Home.tsx`, `Restaurants.tsx`, `Partenaires.tsx`, `Livreurs.tsx` : (1) Remplace tous les **chiffres fictifs** (« Plus de 500 restaurants », « 50K+ commandes/mois », « 27 villes », « 200+ livreurs actifs », « 50 000 FCFA+/semaine », « +30 % de CA ») par des formulations honnêtes et vendeuses sans chiffre inventé — ou par des compteurs branchés sur les données réelles du catalogue quand c'est simple (`restaurants.length`). (2) Corrige l'**entité HTML visible** « R&eacute;el » (Home, chaîne de données) et balaie les autres chaînes de données contenant des entités. (3) Home étape 2 : « Mobile Money ou carte » → « Mobile Money (MTN MoMo, Orange Money) ou espèces à la livraison » ; la mention carte reviendra via `launchConfig.CARD_PAYMENT_AVAILABLE`. (4) Section application mobile : **conserve les badges App Store/Google Play** mais pilote-les par `launchConfig.APP_STORE_URL` / `PLAY_STORE_URL` (null aujourd'hui) : sans lien, badge visible mais non cliquable avec mention « Bientôt disponible » ; avec lien, redevient un lien réel. (5) Témoignages : mention discrète assumée OU 3 avis réels du seed — choisis le plus propre. Vérifie chaque page aux deux tailles et consigne chaque valeur en attente dans le registre de `prompt-master-coordination.md`.

## QA-D — Français irréprochable (C5) — P1

> **[Préambule commun]** Restaure les accents partout où ils manquent à l'écran : (1) tags diététiques et libellés dans `src/lib/dishes.ts` et leurs affichages (`DishResults.tsx`, `DishDetail.tsx`) : « Diabétique, Végétarien, Protéiné, Allégé, Épicé, Braisé, Pressé du jour, Détox… » — attention : si les tags servent de clés de filtrage/matching, normalise la comparaison (fonction de slug/normalisation) pour ne PAS casser le filtrage en ne changeant que l'affichage ; (2) toasts de `Orders.tsx` (« Avis livraison enregistré. », « Avis restaurant enregistré. ») ; (3) libellés d'`AdminReviews.tsx` (« Publiés, Masqués, Signalés, À vérifier, Commande vérifiée, Avis vérifiés issus des commandes livrées »). Vérifie dans Explorer que les filtres diététiques fonctionnent toujours (sélectionner « Végétarien » filtre bien) et que l'admin avis s'affiche correctement.

## QA-E — Gamme de prix en contexte FCFA (C6) — P1

> **[Préambule commun]** Sur `/restaurants` (`src/pages/Restaurants.tsx` et le composant de carte restaurant utilisé), l'indicateur de gamme de prix affiche « € / €€ / €€€ » — incohérent pour une app 100 % FCFA au Cameroun. Remplace par un indicateur sans devise européenne : « ₣ / ₣₣ / ₣₣₣ » ou libellés « Éco · Standard · Premium » (choisis le plus lisible dans la carte, cohérent avec le badge Premium existant — évite le doublon de sens avec ce badge). Mets à jour le tri/filtre éventuel qui s'appuie dessus sans casser son fonctionnement. Vérifie l'affichage des cartes aux deux tailles.

## QA-F — Profil cohérent et sans redondance (C9) — P1

> **[Préambule commun]** Dans `src/pages/Profile.tsx` : (1) pré-remplis le champ nom avec `user.name` (registre) quand `yamo_profile_name` est vide, et propage l'enregistrement comme aujourd'hui ; (2) **fusionne les deux blocs adresse** (« Adresse de livraison » et « Adresses enregistrées ») en une seule section claire : liste des adresses + bouton Ajouter + état vide unique ; (3) supprime les accès redondants « Mes commandes » (garde celui des ACCÈS RAPIDES, retire le bouton isolé en bas de page) ; (4) remplace les emojis des accès rapides (📦❤️🍽️🔍) par les icônes lucide correspondantes (Package, Heart, UtensilsCrossed, Search) dans des pastilles au style des cartes existantes ; (5) corrige le calcul « Restaurant préféré » : le restaurant le PLUS commandé sur l'historique (en nombre de commandes, égalité départagée par le plus récent). Vérifie avec Marie Ngono : nom pré-rempli, une seule section adresse, stats justes.

## QA-G — Route `/plat/:slug` + slugs corrects (C11) — P2

> **[Préambule commun]** (1) Dans `src/lib/dishes.ts`, corrige `dishSlug` pour translittérer les ligatures avant suppression des diacritiques : œ→oe, æ→ae (« Boukarou de Bœuf » → `boukarou-de-boeuf`). (2) Renomme la route `/article/:slug` → `/plat/:slug` dans `src/App.tsx` (c'est ce que CLAUDE.md documente) et mets à jour TOUTES les constructions de liens (`DishResults.tsx`, `DishDetail.tsx`, `Favorites.tsx`, `ActiveOperationsBar.tsx`, `App.tsx` hasOwnCartBar, état `from` de connexion dans `DishDetail`). (3) Ajoute une **redirection** `/article/:slug` → `/plat/:slug` (Navigate replace avec préservation du slug) : les liens WhatsApp déjà partagés doivent continuer de fonctionner, y compris les anciens slugs sans `oe` — si le slug entrant ne matche aucun plat, tente le matching via la normalisation avant d'afficher l'écran « introuvable ». (4) Mets à jour la ligne de route dans CLAUDE.md si écart restant. Vérifie : `/plat/boukarou-de-boeuf` s'affiche, `/article/boukarou-de-buf` (ancien lien) redirige vers la bonne fiche, panier/checkout intacts.

## QA-H — Données & libellés divers (C10, C12, C13) — P2

> **[Préambule commun]** (1) `src/data/mockData.ts` : remplace le quartier « Tokoin » (quartier de Lomé, pas de Yaoundé) par un vrai quartier de Yaoundé (ex. « Mvog-Ada ») — vérifie qu'aucun filtre/quartier codé en dur ne référence Tokoin (`locations.ts`, `cities.ts`). (2) Badges Explorer (`DishResults.tsx`) : « Tendance » ne doit plus apparaître sur la quasi-totalité des plats — limite-le à un critère réel (ex. top 20 % des plats les plus commandés dans `yamo_local_orders`, repli : `isPopular`) ; l'onglet « Nouveautés » ne doit plus égaler « Tous » — soit un critère réel (plats ajoutés récemment), soit suppression de l'onglet. (3) `src/pages/Checkout.tsx` : libellé « Ville(restaurant) » → « Ville de livraison » avec sous-texte expliquant qu'elle est fixée par le restaurant si c'est la logique réelle ; la phrase « Le code sera vérifié à la confirmation de la commande. » doit être contextualisée là où elle s'applique (code promo ? code de livraison ?) ou supprimée si elle n'accompagne aucun champ visible. Vérifie Explorer (badges devenus rares et signifiants) et le checkout complet en mock.

## QA-I — Synchronisation doc + QA générale finale — Gate

> **[Préambule commun]** (1) Mets à jour `CLAUDE.md` : route `/plat/:slug` (si QA-G passé), retire « Messages rapides simulés » du tableau de dette (déjà purgé de `Orders.tsx`), corrige la description de `VITE_FORCE_MOCK_AUTH` (lu nulle part dans src/ — seul `VITE_USE_VPS_API` est actif). (2) QA générale : sur le serveur mock seedé, déroule les 4 parcours — client (Marie Ngono : explorer → fiche plat → panier → checkout jusqu'à l'écran de paiement, commandes, profil, favoris), restaurateur démo (+237690000003 : accepter une commande pending), livreur démo (+237690000005 : gains + course en cours), admin (+237690000001 : customers, orders, reviews, applications, drivers) — en desktop ET mobile 360px. (3) Vérifie sur chaque page auditée que les constats C1→C13 sont résolus (grille de `audit-qualite-pages.md`) et qu'aucune régression n'apparaît (console propre, pas d'overflow). (4) Livre un rapport final : tableau des constats avec statut Résolu/Reporté + captures des preuves clés. `npm run lint` + `npm run build` doivent passer.
